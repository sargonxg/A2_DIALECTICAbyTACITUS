# GCS backend for Terraform state — shared across team.
# Uncomment and configure for production use.

# terraform {
#   backend "gcs" {
#     bucket = "dialectica-terraform-state"
#     prefix = "terraform/state"
#   }
# }
