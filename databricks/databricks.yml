bundle:
  name: tacitus-dialectica-core-v1

include:
  - resources/jobs.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
